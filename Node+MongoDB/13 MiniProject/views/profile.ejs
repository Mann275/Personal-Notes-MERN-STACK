<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex items-end justify-end">
        <a
          href="/logout"
          class="bg-red-500 rounded-md px-3 py-2 mb-5 inline-block"
          onclick="return confirm('Are you sure you want to log out?')"
          >LogOut</a
        >
      </div>
      <div class="flex items-start gap-3 ">
        <div class="w-10 h-10 rounded-full overflow-hidden"> 
          <img class="w-full h-full rounded-full object-cover" src="/images/upload/<%= user.profilePic %>" alt="">
        </div>
      <h2 class="text-3xl font-bold mb-5">
        <span class="font-light">Welcome</span>, <%= user.name %> 🙂
      </h2></div>
      <h5 class="mb-5 mt-3">You can create a new post</h5>
      <form action="/post" method="post">
        <textarea
          name="content"
          class="w-1/3 resize-none bg-transparent border-2 border-zinc-700 rounded-md p-2 outline-none block"
          placeholder="Whats on your mind?"
        ></textarea>
        <input
          type="submit"
          name=""
          value="Create New Post"
          class="px-3 text-sm py-2 rounded-md w-40 mt-2 bg-blue-500 block"
        />
      </form>
      <div class="post mt-20">
        <h3 class="text-zinc-400">Your Posts</h3>
        <div class="postcontainer mt-5">
            <% user.posts.reverse().forEach(post => {%>
                          <div
            class="post w-1/3 p-4 border-1 mb-4 rounded-md bg-zinc-800 border-zinc-600"
          >
            <h2 class="text-blue-500 mb-2"><%= user.username %></h2>
            <p class="text-sm tracking-tight">
              <%= post.content %>
            </p>
            <small class="mt-4 inline-block"><%= post.likes.length %> likes</small>
            <div class="btns mt-5 flex items-center justify-between">
                <div class="flex gap-4">
                    <a class="text-blue-500" href="/like/<%= post._id %>">
                    <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                    </a>
                    <a class="text-zinc-300" href="/edit/<%= post._id %>">Edit</a>
                </div>
                
                <a 
                    href="/delete/<%= post._id %>" 
                    class="text-white"
                    onclick="return confirm('Are you sure you want to delete this post?')"
                >
                    🗑️
                </a>
                </div>

          </div>

             <% })%>

        </div>
      </div>
    </div>
  </body>
</html>
